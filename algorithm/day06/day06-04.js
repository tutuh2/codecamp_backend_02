function solution(arr) {
    // var answer = 0;
    
    // for( let value of arr ){
    //     answer += value;
    // }
    // answer = answer/(arr.length)
    // return answer;

    const sum = arr.reduce( (acc, cur) => {
        console.log(acc, cur);
    }, 0)
}